import { schemaMigrations } from '@nozbe/watermelondb/Schema/migrations';

// Add new migrations here as schema evolves.
// Each migration increments the schema version.
export const migrations = schemaMigrations({
  migrations: [],
});
